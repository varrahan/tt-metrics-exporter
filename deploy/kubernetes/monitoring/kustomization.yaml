apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-monitor.yaml
  - prometheus-rules.yaml
configMapGenerator:
  - name: tt-metrics-exporter-dashboard
    files:
      - dashboard.json
generatorOptions:
  labels:
    grafana_dashboard: "1"
